# 델타이용

* delta 좌표계 이용, vector 활용

## snail

```python
snail = [[0] * 5 for i in range(5)]

X, Y = 0, 0
dx = [1, 0, -1, 0] # 우 => 하 => 좌 => 상 순으로 순회
dy = [0, 1, 0, -1]
dir_stat = 0

def isWall(x, y):
    if x < 0 or x >= 5: # 초기 벽 세팅
        return True
    if y < 0 or y >= 5: # 초기 벽 세팅
        return True
    if snail[y][x] != 0: # 초기 지나고 나서 벽 세팅
        return True

# rand_lst에는 1~25까지의 정보가 정렬되어 담겨져 있음
for i in range(5):
    for j in range(5):
        print(Y, X)
        snail[Y][X] = rand_lst[i][j]
        X += dx[dir_stat]
        Y += dy[dir_stat]
        if isWall(X, Y):
            X -= dx[dir_stat] # 벽 만나면 방향 바꾼 후 더해줌
            Y -= dy[dir_stat] # 벽 만나면 방향 바꾼 후 더해줌
            dir_stat = (dir_stat + 1) % 4 # 우 => 하 => 좌 => 상 순으로 순회
            X += dx[dir_stat]
            Y += dy[dir_stat]
print(snail)
```



